---
phase: 01-p0-core-freeze
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Engine/Graphics/source/Renderer/ShadowMapPass.cpp
  - Engine/Graphics/include/Pyramid/Graphics/Renderer/ShadowMapPass.hpp
  - Engine/Graphics/source/Renderer/DeferredLightingPass.cpp
  - Tests/ShadowArrayTests.cpp
  - docs/Architecture.md
  - CHANGELOG.md
autonomous: false
requirements: [FRZ-02]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An agent using deferred shadow-map-array bindings gets complete documented behavior: all cascades render into array layers and the lighting pass binds the single array once with matching sampler type (FRZ-02)"
    - "The lighting pass uploads light-space matrices, cascade splits, and cascade count alongside the bind, so shadows are not stuck on cascade zero with identity transforms (FRZ-02)"
    - "Shadow resolution stays window-size-independent and the after-pass surface restore convention is preserved by every touched pass (FRZ-02)"
    - "ASSUMED (unresolved specless probe FRZ-02/adjacency): a cascade count of exactly one behaves like the general path with a single layer, never a special-case bind"
    - "ASSUMED (unresolved specless probe FRZ-02/empty): zero cascades or an empty shadow-map set skips the bind with lighting continuing unshadowed, never a bind of an invalid handle"
    - "ASSUMED (unresolved specless probe FRZ-02/ordering): cascade layer order matches light-space-matrix and split order deterministically, so layer index always addresses the intended cascade"
  artifacts:
    - "Engine/Graphics/source/Renderer/ShadowMapPass.cpp owning one GL_TEXTURE_2D_ARRAY depth texture"
    - "Engine/Graphics/source/Renderer/DeferredLightingPass.cpp binding the array once with full uniform uploads"
    - "Tests/ShadowArrayTests.cpp proving completeness, layer count, single bind, and upload presence"
  key_links:
    - "ShadowMapPass array layers to DeferredLightingPass bind (target GL_TEXTURE_2D_ARRAY matches sampler2DArray)"
    - "ShadowMapPass GetLightSpaceMatrices and cascade splits to lighting-pass uniform uploads (no missing matrices)"
    - "Touched passes to the RenderSystem after-pass restore convention (no leaked framebuffer or viewport state)"
  prohibitions:
    - "Never leave an advertised capability silently unperformed"
    - "Never add a new engine capability disguised as a freeze fix"
    - "Never mutate published immutable resources in place"
---

<objective>
Complete deferred shadow-map-array binding on the locked 3.3 baseline with CPU-side-only changes: one GL_TEXTURE_2D_ARRAY depth texture with cascade layers, a single bind matching the shaders sampler2DArray, and the missing matrix, split, and count uploads — per CONTEXT implementation delegation and the researcher IMPLEMENT recommendation. Shaders already declare uniform sampler2DArray u_ShadowMaps with correct GLSL 3.30 texture() overloads, so no shader rewrite is needed.

Purpose: Multi-cascade shadows stop being silently wrong (single 2D bind against an array sampler).
Output: Array-owning shadow pass, array-binding lighting pass with full uploads, headless tests, human visual sign-off.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0-core-freeze/01-CONTEXT.md
@.planning/phases/01-p0-core-freeze/01-RESEARCH.md
@.planning/phases/01-p0-core-freeze/01-PATTERNS.md
@.planning/phases/01-p0-core-freeze/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Bind a one-layer shadow array end to end with matrix uploads</name>
  <files>Engine/Graphics/source/Renderer/ShadowMapPass.cpp, Engine/Graphics/source/Renderer/DeferredLightingPass.cpp, Tests/ShadowArrayTests.cpp</files>
  <read_first>Engine/Graphics/source/Renderer/ShadowMapPass.cpp lines 61-100; Engine/Graphics/source/Renderer/DeferredLightingPass.cpp lines 100-180; Engine/Graphics/shaders/deferred_lighting.frag lines 12-16 and 96-99; Tests/FramebufferResizeTests.cpp lines 11-59 for the fake-GL harness shape; 01-PATTERNS.md ShadowMapPass plus DeferredLightingPass sections</read_first>
  <action>Own one GL_TEXTURE_2D_ARRAY depth texture sized 2048 by 2048 by 1 with GL_DEPTH_COMPONENT24, GL_NEAREST filtering, and GL_CLAMP_TO_BORDER with white border, created via glTexImage3D reusing the existing per-cascade parameters; populate layer zero from the single cascade render; replace the first-cascade GL_TEXTURE_2D bind in DeferredLightingPass SetShadowMaps with a single BindNativeTexture of the array on unit 5 with target GL_TEXTURE_2D_ARRAY keeping the u_ShadowMaps uniform name and slot; add the missing uploads of u_LightSpaceMatrices from ShadowMapPass GetLightSpaceMatrices and u_CascadeCount in the same SetUniform idiom as the existing u_ShadowBias line; run OpenGLDiagnostics CheckError after array creation and first bind; keep shadow resolution fixed at 2048 independent of window size; create Tests/ShadowArrayTests.cpp copying the fake-GL harness shape with file-local g_capture globals, Fake entry points, and suite-prefixed Fail, asserting array completeness, layer count of one, bind target equals GL_TEXTURE_2D_ARRAY, and matrix and count upload presence. Researcher implement path, blit-vs-layered choice deferred to Task 2.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Shadow|Framebuffer|Lighting" }</automated></verify>
  <acceptance_criteria>Source assertions: no BindNativeTexture with GL_TEXTURE_2D remains on the shadow path; u_LightSpaceMatrices and u_CascadeCount uploads exist in the deferred Execute body. Behavior assertions: new ShadowArrayTests prove single-layer completeness, exact-once array bind with correct target, and upload presence; existing framebuffer suites pass; engine plus tests build with zero errors.</acceptance_criteria>
  <done>One shadow cascade flows end to end through an array texture into correctly transformed lighting, proven headless.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to N cascades with splits upload and restore preservation</name>
  <files>Engine/Graphics/source/Renderer/ShadowMapPass.cpp, Engine/Graphics/include/Pyramid/Graphics/Renderer/ShadowMapPass.hpp, Engine/Graphics/source/Renderer/DeferredLightingPass.cpp, Tests/ShadowArrayTests.cpp</files>
  <read_first>Engine/Graphics/source/Renderer/ShadowMapPass.cpp full cascade creation and Execute body; Engine/Graphics/include/Pyramid/Graphics/Renderer/ShadowMapPass.hpp member layout; Engine/Graphics/source/Renderer/RenderSystem.cpp lines 285-309 restore convention</read_first>
  <action>Generalize the array to 2048 by 2048 by cascade count with transactional recreation when cascade count or resolution changes, preserving the previous texture on failure; populate layers per frame by rendering each cascade directly into its layer via glFramebufferTextureLayer, falling back to keep-N-framebuffers plus depth glBlitFramebuffer into layers if layered rendering proves driver-risky, and record the chosen method in code comments; upload u_CascadeSplits with plus-one element from the pass splits source alongside matrices and count; validate cascade count is greater than zero and within GL_MAX_ARRAY_TEXTURE_LAYERS, skipping the bind with lighting continuing unshadowed on empty input; preserve the after-pass framebuffer plus viewport restore lines in every touched pass without migrating them to the neutral interface (that migration belongs to plan 01-04); extend ShadowArrayTests with layer-count-equals-cascade-count, all-cascades-bound-exactly-once, splits upload presence, recreation-on-count-change, empty-set skip, and restoration assertions.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Shadow|Framebuffer|Render" }</automated></verify>
  <acceptance_criteria>Source assertions: array depth equals cascade count with transactional recreation; splits upload with plus-one element exists; cascade-count validation precedes creation; restore lines remain in every touched pass. Behavior assertions: extended ShadowArrayTests prove multi-cascade completeness, single bind, all three uploads, recreation, empty skip, and restore preservation; engine plus tests build with zero errors.</acceptance_criteria>
  <done>All cascades bind through one array with full uploads, invalid and empty inputs behave explicitly, and no pass leaks framebuffer or viewport state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual inspection of multi-cascade shadows</name>
  <what-built>Multi-cascade shadow-array binding: one GL_TEXTURE_2D_ARRAY depth texture bound once with matrices, splits, and count uploaded; Examples/BasicRendering builds with the fix</what-built>
  <how-to-verify>
    Run Examples/BasicRendering on a real GPU and verify:
    1. Shadowed geometry renders across all cascades with no popping at cascade transitions
    2. No shadow acne or peter-panning beyond the pre-change baseline look
    3. Resize during shadow rendering leaves no viewport or framebuffer corruption
    4. Screenshots are captured from the reference rendering path for the record
  </how-to-verify>
  <resume-signal>Type "approved" or describe shadow defects with the cascade where they appear</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cascade-config→array-creation | Cascade count and resolution flow into glTexImage3D sizing and must be validated |
| gpu-state→later-passes | Array framebuffer and viewport changes leak into world and UI passes if restore is skipped |
| shader-sampling→bound-texture | Sampler-type and texture-target mismatch samples undefined data silently on some drivers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of service | ShadowMapPass array creation | medium | mitigate | Validate cascade count above zero and within GL_MAX_ARRAY_TEXTURE_LAYERS plus IsValidExtent-style extent checks; transactional recreation preserves the previous texture on failure; tests cover empty and oversized inputs |
| T-03-02 | Tampering | DeferredLightingPass bind | medium | mitigate | Assert bind target equals GL_TEXTURE_2D_ARRAY matching sampler2DArray; CheckError after creation and first bind; single-bind test plus exact-once assertion |
| T-03-03 | Information disclosure | later render passes and UI | low | mitigate | Preserve after-pass framebuffer plus viewport restore lines in every touched pass; restoration tests extended for the shadow path |
| T-03-04 | Repudiation | cascade layer ordering | low | mitigate | Layer index follows matrix and split order deterministically; layer-count and splits-presence tests pin the correspondence |
| T-03-SC | Tampering | package installs | low | accept | No package installs in this plan; all entry points come from vendored vendor/glad, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
ShadowArrayTests plus framebuffer plus render suites green on debug; CheckError clean on array creation and first bind; human visual sign-off recorded for cascade transitions and post-resize state.
</verification>

<success_criteria>
An agent using deferred shadow bindings gets every cascade through one correctly typed array bind with matrices, splits, and count uploaded; empty and invalid inputs behave explicitly; a human has confirmed the pixels (FRZ-02 satisfied).
</success_criteria>

<artifacts_produced>
Symbols created: Tests/ShadowArrayTests.cpp suite (array completeness, layer count, single bind, upload presence, recreation, empty skip, restore assertions); no new public engine API — array ownership is renderer-internal. Symbols modified: ShadowMapPass cascade storage and creation, DeferredLightingPass SetShadowMaps plus uniform uploads. Docs entries in docs/Architecture.md and CHANGELOG.md if behavior notes are warranted.
</artifacts_produced>

<output>
Create `.planning/phases/01-p0-core-freeze/01-03-SUMMARY.md` when done
</output>
