---
phase: 01-p0-core-freeze
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - Engine/Graphics/include/Pyramid/Graphics/Framebuffer.hpp
  - Engine/Graphics/include/Pyramid/Graphics/GraphicsDevice.hpp
  - Engine/Graphics/source/OpenGL/OpenGLDevice.cpp
  - Engine/Graphics/source/OpenGL/OpenGLFramebuffer.cpp
  - Engine/Graphics/include/Pyramid/Graphics/OpenGL/OpenGLFramebuffer.hpp
  - Engine/Graphics/source/Renderer/RenderSystem.cpp
  - Engine/Graphics/source/Renderer/ShadowMapPass.cpp
  - Engine/Graphics/source/Renderer/DeferredLightingPass.cpp
  - Engine/Graphics/source/Texture.cpp
  - Tests/PublicApiLinkage.cpp
  - Tests/FramebufferResizeTests.cpp
  - Tests/TextureLoadingTests.cpp
  - docs/Architecture.md
  - CHANGELOG.md
autonomous: false
requirements: [FRZ-05]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "An agent holding an IFramebuffer object can bind it through the backend-neutral IGraphicsDevice interface and get real binding with symmetric error state, never a not-yet-implemented stub (FRZ-05, per D-03)"
    - "RenderSystem restore, RenderTarget bind and unbind, and pass binds route through the neutral method so the restore contract is backend-portable (FRZ-05, per D-03)"
    - "An agent creating a depth texture through ITexture2D CreateDepthTarget gets a real sampled depth texture for a mapped depth or stencil format, transactionally, with explicit failure on invalid extents (FRZ-05, per D-03)"
    - "D-04: renderer change verified per D-04 code-vs-hardware discretion bound — every code change covered by Tests/FramebufferResizeTests.cpp and Tests/TextureLoadingTests.cpp plus blocking human visual inspection of framebuffer routing and depth targets"
    - "ASSUMED (unresolved specless probe FRZ-05/unclassified, manual review): the minimal IFramebuffer surface of Bind, Unbind, IsComplete, and size plus an opaque handle covers every in-phase call site with no speculative growth"
    - "ASSUMED (unresolved specless probe FRZ-02/ordering carried into restore routing): restore order of framebuffer-then-viewport is preserved exactly during migration, so pass sequencing behavior is unchanged"
  artifacts:
    - "Engine/Graphics/include/Pyramid/Graphics/Framebuffer.hpp defining IFramebuffer"
    - "Engine/Graphics/source/OpenGL/OpenGLDevice.cpp with a real BindFramebuffer implementation"
    - "Engine/Graphics/source/Texture.cpp with CreateDepthTarget routed through OpenGLTexture2D"
    - "Tests/PublicApiLinkage.cpp pinning the new IFramebuffer surface"
  key_links:
    - "IFramebuffer definition to OpenGLFramebuffer implementation to OpenGLDevice BindFramebuffer (declaration, override, and device routing agree)"
    - "RenderSystem and RenderTarget call sites to the neutral BindFramebuffer method (no pass holds a raw GLuint across the boundary)"
    - "CreateDepthTarget to the plan 01-02 depth and stencil format mappings (creation reuses the verified triples)"
    - "Plan 01-03 shadow-array binds to the neutral BindFramebuffer migration (single-array-bind plus upload behavior preserved across the routing change)"
  prohibitions:
    - "Never leave an advertised capability silently unperformed"
    - "Never add a new engine capability disguised as a freeze fix"
    - "Never mutate published immutable resources in place"
---

<objective>
Define the missing IFramebuffer interface, implement backend-neutral binding through it, route RenderSystem restore plus RenderTarget plus pass binds off raw handles, and implement CreateDepthTarget through the texture interface reusing the plan 01-02 depth mappings — per D-03 researcher recommendation IMPLEMENT both halves. IFramebuffer is currently declared but defined nowhere, making the neutral method uncallable with any real object.

Purpose: Backend-neutral framebuffer binding becomes trustworthy, and depth-texture creation works through the texture interface.
Output: New IFramebuffer header, real device bind, routed call sites, working depth-target factory, linkage pins, tests, docs.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0-core-freeze/01-CONTEXT.md
@.planning/phases/01-p0-core-freeze/01-RESEARCH.md
@.planning/phases/01-p0-core-freeze/01-PATTERNS.md
@.planning/phases/01-p0-core-freeze/01-VALIDATION.md
@.planning/phases/01-p0-core-freeze/01-02-SUMMARY.md
@.planning/phases/01-p0-core-freeze/01-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Define IFramebuffer and bind it end to end through the device</name>
  <files>Engine/Graphics/include/Pyramid/Graphics/Framebuffer.hpp, Engine/Graphics/include/Pyramid/Graphics/GraphicsDevice.hpp, Engine/Graphics/source/OpenGL/OpenGLDevice.cpp, Engine/Graphics/include/Pyramid/Graphics/OpenGL/OpenGLFramebuffer.hpp, Engine/Graphics/source/OpenGL/OpenGLFramebuffer.cpp, Tests/FramebufferResizeTests.cpp, Tests/PublicApiLinkage.cpp</files>
  <read_first>Engine/Graphics/include/Pyramid/Graphics/GraphicsDevice.hpp lines 267-276; Engine/Graphics/source/OpenGL/OpenGLDevice.cpp lines 368-409; Engine/Graphics/source/OpenGL/OpenGLFramebuffer.cpp lines 23-80; Tests/FramebufferResizeTests.cpp lines 11-59; Tests/PublicApiLinkage.cpp lines 197-249; 01-PATTERNS.md GraphicsDevice plus OpenGLDevice plus linkage sections</read_first>
  <action>Create Engine/Graphics/include/Pyramid/Graphics/Framebuffer.hpp defining class IFramebuffer with a minimal GLAD-free and Win32-free surface of Bind, Unbind, IsComplete, GetWidth, GetHeight, plus an opaque u32 native handle accessor, adding no speculative methods and no required method with a silent no-op default; declare OpenGLFramebuffer as implementing IFramebuffer; implement OpenGLDevice BindFramebuffer so the non-null branch calls framebuffer Bind via OpenGLStateManager and clears m_lastError symmetrically while the null branch keeps binding framebuffer zero; add linkage pin lines for the new IFramebuffer surface in Tests/PublicApiLinkage.cpp using the volatile decltype pin idiom; extend Tests/FramebufferResizeTests.cpp in the existing fake-GL harness style with neutral-bind real-object, null-bind default, and symmetric error-state tests. Depends on plan 01-01 for conflict-free linkage edits, on plan 01-02 for texture-mapping sequencing (not interface content), and on plan 01-03 for wave ordering ahead of the Task 2 shadow-bind migration. Per D-03 implement direction.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Framebuffer|PublicApi" }</automated></verify>
  <acceptance_criteria>Source assertions: IFramebuffer is defined in a public header with no GLAD or Win32 includes; OpenGLFramebuffer implements it; the BindFramebuffer stub message is gone; linkage pins exist for the new surface. Behavior assertions: neutral bind of a real object, null bind of default, and error-state symmetry tests pass; framebuffer plus linkage suites pass; engine plus tests build with zero errors.</acceptance_criteria>
  <done>One framebuffer object binds end to end through the neutral interface with tests proving real binding and clean error state.</done>
</task>

<task type="auto">
  <name>Task 2: Route call sites through neutral bind</name>
  <files>Engine/Graphics/source/Renderer/RenderSystem.cpp, Engine/Graphics/source/Renderer/ShadowMapPass.cpp, Engine/Graphics/source/Renderer/DeferredLightingPass.cpp, Tests/FramebufferResizeTests.cpp</files>
  <read_first>Engine/Graphics/source/Renderer/RenderSystem.cpp lines 285-309; Engine/Graphics/source/Renderer/ShadowMapPass.cpp cascade creation and Execute body plus Engine/Graphics/source/Renderer/DeferredLightingPass.cpp shadow-bind site (plan 01-03 array binds and restores to migrate, honoring the layered-vs-blit method recorded in the 01-03 SUMMARY); 01-PATTERNS.md restore-convention section</read_first>
  <action>Migrate the RenderSystem Render restore lines plus RenderTarget Bind and Unbind plus remaining pass-site raw BindFramebufferHandle calls onto the neutral BindFramebuffer with IFramebuffer objects or nullptr, keeping the two-line restore comment shape and exact framebuffer-then-viewport order and keeping BindFramebufferHandle only as the device-internal workhorse; explicitly migrate plan 01-03's new ShadowMapPass array binds and DeferredLightingPass array restores onto the neutral method, preserving the single-array-bind plus matrix, split, and count upload behavior and the restore lines exactly as 01-03 left them; extend Tests/FramebufferResizeTests.cpp in the existing fake-GL harness style with neutral-restore assertions for each touched pass. Per D-03 implement direction.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Framebuffer|Render" }</automated></verify>
  <acceptance_criteria>Source assertions: no pass or system call site outside the device holds a raw GLuint across the boundary, including the migrated ShadowMapPass and DeferredLightingPass array binds with 01-03 single-bind plus upload behavior preserved. Behavior assertions: neutral-restore tests for each touched pass pass; engine plus tests build with zero errors.</acceptance_criteria>
  <done>All binding flows through the neutral interface with restore order preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Implement depth targets through the texture interface</name>
  <files>Engine/Graphics/source/Texture.cpp, Engine/Graphics/source/OpenGL/OpenGLFramebuffer.cpp, Tests/TextureLoadingTests.cpp, docs/Architecture.md, CHANGELOG.md</files>
  <read_first>Engine/Graphics/source/Texture.cpp lines 12-45; Engine/Graphics/source/OpenGL/OpenGLFramebuffer.cpp lines 109, 209, and 933 onward for extent guards; 01-PATTERNS.md factory plus input-validation sections</read_first>
  <action>Implement ITexture2D CreateDepthTarget through OpenGLTexture2D for the plan 01-02 mapped depth and stencil set with compare mode NONE default, following the transactional validate-extent then allocate then completeness-check then rollback shape that preserves nothing half-made and logs with dimensions on failure; keep OpenGLFramebuffer depth attachments as the render-target path and document the coexistence of sampled depth texture versus attachment in docs/Architecture.md textures section (this plan is the Wave-2 docs owner) plus a CHANGELOG.md entry consolidating this plan's routing notes with the changelog entry text from the 01-02 SUMMARY and any 01-03 behavior notes; extend Tests/TextureLoadingTests.cpp with per-depth-format CreateDepthTarget cases plus zero and oversized extent explicit-failure cases; publish depth textures through ResourceRegistry Textures with no cached-instance mutation; run ResourceHandleTests, ResourceManifestTests, and ResourceRegistryTests. Per D-03 implement direction.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Texture|Resource" }</automated></verify>
  <acceptance_criteria>Source assertions: CreateDepthTarget routes through OpenGLTexture2D for mapped depth formats; docs distinguish sampled depth texture from attachment usage; CHANGELOG.md carries consolidated entries for plans 01-02, 01-03 (if noted), and 01-04. Behavior assertions: per-depth-format creation plus zero plus oversized explicit-failure tests pass; handle, manifest, and registry suites pass; engine plus tests build with zero errors.</acceptance_criteria>
  <done>Depth targets create through the texture interface with transactional safety.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual inspection of framebuffer routing and depth targets</name>
  <what-built>Neutral framebuffer binding migration: RenderSystem restore plus RenderTarget plus pass binds route through IGraphicsDevice BindFramebuffer; depth targets create through the texture interface; both Examples/BasicGame and Examples/BasicRendering build with the migration</what-built>
  <how-to-verify>
    Run both examples on a real GPU and verify:
    1. Rendering is correct with no viewport corruption and no leaked off-screen state into UI
    2. Resize, minimize-restore, and close sequence behave per the docs/ROADMAP.md P0 Windows runtime verification checklist subset
    3. Shadowed rendering looks correct after the routing migration
    4. Screenshots are captured from the reference rendering path for the record
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering defects with the lifecycle step where they appear</resume-signal>
  <acceptance_criteria>D-04: blocking human visual inspection implements the D-04 renderer-changes-need-human-verification bound; sign-off recorded for routing migration and lifecycle behavior with every code change covered by Tests/FramebufferResizeTests.cpp and Tests/TextureLoadingTests.cpp.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pass-code→device-bind | Passes hand framebuffer objects across the backend-neutral boundary; raw handles must not leak past it |
| caller-extents→depth-creation | Width, height, and depth format arrive from callers and may be hostile |
| incomplete-fbo→render-target | Binding an incomplete framebuffer redirects rendering into undefined targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | OpenGLDevice BindFramebuffer | medium | mitigate | Non-null branch binds via OpenGLStateManager and clears m_lastError symmetrically; null branch binds zero; fake-GL tests prove both plus error symmetry |
| T-04-02 | Denial of service | CreateDepthTarget extents | medium | mitigate | FramebufferUtils extent guards plus transactional create-then-swap; zero and oversized tests must fail explicitly never crash |
| T-04-03 | Tampering | RenderSystem and pass call sites | medium | mitigate | Route every site through the neutral method preserving restore order; restoration tests extended for each touched pass; human visual sign-off covers leaks |
| T-04-04 | Spoofing | depth-texture cache identity | low | mitigate | Reuse plan 01-02 depth mapping triples and byte sizing; publish only through ResourceRegistry Textures; handle, manifest, and registry suites must pass |
| T-04-SC | Tampering | package installs | low | accept | No package installs in this plan; sole runtime library is vendored vendor/glad, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
Framebuffer, texture, resource, render, and linkage suites green on debug; depth-target per-format plus zero plus oversized fixtures behave explicitly; human visual sign-off recorded for routing migration and lifecycle behavior.
</verification>

<success_criteria>
An agent binding any framebuffer through the neutral interface gets real binding; an agent creating any mapped depth target through the texture interface gets a working sampled texture; restores are backend-portable; a human has confirmed the pixels (FRZ-05 satisfied).
</success_criteria>

<artifacts_produced>
Symbols created: class IFramebuffer with Bind, Unbind, IsComplete, GetWidth, GetHeight, and opaque native handle accessor in Engine/Graphics/include/Pyramid/Graphics/Framebuffer.hpp; working ITexture2D::CreateDepthTarget behavior for the mapped depth and stencil set. Symbols modified: OpenGLDevice::BindFramebuffer, OpenGLFramebuffer (IFramebuffer implementation), RenderSystem restore path, RenderTarget bind paths. Test artifacts: neutral-bind, restore, and depth-target cases in Tests/FramebufferResizeTests.cpp and Tests/TextureLoadingTests.cpp. Docs entries in docs/Architecture.md textures section and CHANGELOG.md.
</artifacts_produced>

<output>
Create `.planning/phases/01-p0-core-freeze/01-04-SUMMARY.md` when done
</output>
