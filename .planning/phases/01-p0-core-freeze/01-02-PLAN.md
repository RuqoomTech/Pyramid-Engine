---
phase: 01-p0-core-freeze
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Engine/Graphics/include/Pyramid/Graphics/Texture.hpp
  - Engine/Graphics/source/OpenGL/OpenGLTexture.cpp
  - Engine/Graphics/source/Texture/TextureResource.cpp
  - Tests/TextureLoadingTests.cpp
  - Tests/TextureCacheTests.cpp
  - docs/Architecture.md
autonomous: false
requirements: [FRZ-04]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An agent using any advertised texture format gets a working mapping: float, depth, packed depth-stencil, and single-channel formats upload with type-correct data instead of failing or reinterpreting bytes (FRZ-04, per D-03)"
    - "An agent requesting BC7 gets an explicit compile-time or loud runtime signal that it is unsupported on the 3.3 baseline, never a silent mis-mapping (FRZ-04, per D-03)"
    - "An agent uploading S3TC data on a driver without the extension gets an explicit failure naming the missing extension, never undefined sampling (FRZ-04, per D-03)"
    - "No unmapped enum value remains reachable from agent-visible API: every TextureFormat value either maps or is pruned/gated with a documented diagnostic (FRZ-04, per D-03)"
    - "ASSUMED (unresolved specless probe FRZ-04/adjacency): textures at exactly the maximum valid extent behave like any valid extent; over-limit extents fail explicitly via IsValidExtent"
    - "ASSUMED (unresolved specless probe FRZ-04/empty): zero-extent creation fails explicitly with a diagnostic, never a crash or empty handle"
    - "ASSUMED (unresolved specless probe FRZ-04/ordering): equal-priority format overloads resolve deterministically; sRGB intent keeps its existing precedence"
  artifacts:
    - "Engine/Graphics/source/OpenGL/OpenGLTexture.cpp with full ResolveFormats mapping and type-aware uploads"
    - "Engine/Graphics/source/Texture/TextureResource.cpp with lockstep ResolveBaseFormat and byte-size coverage"
    - "Engine/Graphics/include/Pyramid/Graphics/Texture.hpp with BC7 pruned"
    - "Tests/TextureLoadingTests.cpp with per-format round trips plus malformed, oversized, and limit fixtures"
  key_links:
    - "ResolveFormats internalFormat/dataFormat/type triple to glTexImage2D and SetData upload calls (every non-8-bit format pairs its type)"
    - "OpenGLTexture2D byte sizing to TextureResource ResolveBaseFormat identity (cache sizing stays consistent)"
    - "S3TC mapping arms to the GLAD_GL_EXT_texture_compression_s3tc runtime flag (no unchecked compressed upload)"
  prohibitions:
    - "Never leave an advertised capability silently unperformed"
    - "Never add a new engine capability disguised as a freeze fix"
    - "Never mutate published immutable resources in place"
---

<objective>
Map every 3.3-core advertised texture format to a correct internal-format, data-format, and upload-type triple, prune BC7 (OpenGL 4.2-only, unmappable on the locked baseline), and gate S3TC behind a runtime extension check — per D-03 researcher recommendation MAP-first, prune-only-BC7, gate-S3TC.

Purpose: No unmapped enum value reaches agent-visible API; every mapping uploads type-correct data.
Output: Extended ResolveFormats plus type-aware uploads plus cache mirror, per-format tests with malformed and limit fixtures, BC7 prune with changelog, S3TC gate with absent-path test.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0-core-freeze/01-CONTEXT.md
@.planning/phases/01-p0-core-freeze/01-RESEARCH.md
@.planning/phases/01-p0-core-freeze/01-PATTERNS.md
@.planning/phases/01-p0-core-freeze/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Map one float format end to end through upload and cache</name>
  <files>Engine/Graphics/source/OpenGL/OpenGLTexture.cpp, Engine/Graphics/source/Texture/TextureResource.cpp, Tests/TextureLoadingTests.cpp</files>
  <read_first>Engine/Graphics/source/OpenGL/OpenGLTexture.cpp lines 14-78, 214-223, and 290-314 plus 384-429; Engine/Graphics/source/Texture/TextureResource.cpp lines 98-133; Tests/TextureLoadingTests.cpp lines 15-67; 01-PATTERNS.md OpenGLTexture plus cache-mirror plus fake-GL harness sections</read_first>
  <action>Add the RGBA16F arm to OpenGLTexture2D ResolveFormats following the existing internalFormat plus dataFormat plus bytesPerPixel plus return-true shape, verifying the GL_RGBA16F, GL_RGBA pairing against vendor/glad/include/glad/glad.h tokens and spec tables 3.12 through 3.14; thread a per-format pixel type through both upload sites so RGBA16F uses GL_FLOAT instead of the hardcoded GL_UNSIGNED_BYTE while keeping the surrounding glPixelStorei UNPACK_ALIGNMENT 1 plus CheckError plus ApplyParameters sequence; mirror the arm in TextureResource ResolveBaseFormat with matching bytes-per-pixel so cache identity and sizing stay consistent; extend Tests/TextureLoadingTests.cpp in the existing file-local g_capture plus Fake plus Fail harness style with an RGBA16F round-trip test asserting captured internal format, data format, type, and byte size plus a malformed and an oversized-extent case that must fail explicitly; reuse IsValidExtent and CalculateByteSize overflow guards without weakening them. Per D-03 map direction.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Texture" }</automated></verify>
  <acceptance_criteria>Source assertions: ResolveFormats contains an RGBA16F arm verified against glad.h tokens; no hardcoded GL_UNSIGNED_BYTE remains on the RGBA16F upload path; ResolveBaseFormat mirrors the arm. Behavior assertions: texture suites pass including the new RGBA16F round trip and the explicit-failure malformed and oversized cases; engine plus tests build with zero errors.</acceptance_criteria>
  <done>One float format proves the full mapping pattern end to end: resolve, type-correct upload, cache identity, and tested failure paths.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm BC7 enum prune before executing it</name>
  <decision>Whether to delete the TextureFormat BC7_RGBA enum value (one-way contract break) or keep it as a loud explicit-failure arm</decision>
  <context>Deleting TextureFormat enum values breaks the published texture contract per D-03, and re-adding a value later is a contract migration. BPTC/BC7 went core only in OpenGL 4.2 so it is unmappable on the locked 3.3 baseline, which is the evidence for pruning; the alternative keeps the value but fails loudly at runtime naming the baseline limitation.</context>
  <options>
    <option id="prune">
      <name>Prune BC7_RGBA from the enum</name>
      <pros>Compile-time signal for agents; no dead value reachable from agent-visible API; matches researcher recommendation</pros>
      <cons>One-way contract break requiring a CHANGELOG entry and docs note; re-adding later is a migration</cons>
    </option>
    <option id="keep-explicit-failure">
      <name>Keep BC7_RGBA with explicit failure</name>
      <pros>No contract break; surface stays stable for a future 4.2-plus backend</pros>
      <cons>Agents discover support only by trial at runtime; dead surface persists on the frozen core</cons>
    </option>
  </options>
  <resume-signal>Select: prune or keep-explicit-failure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Complete the format table with S3TC gate, BC7 fate, and limit fixtures</name>
  <files>Engine/Graphics/include/Pyramid/Graphics/Texture.hpp, Engine/Graphics/source/OpenGL/OpenGLTexture.cpp, Engine/Graphics/source/Texture/TextureResource.cpp, Tests/TextureLoadingTests.cpp, Tests/TextureCacheTests.cpp, docs/Architecture.md</files>
  <read_first>Engine/Graphics/include/Pyramid/Graphics/Texture.hpp lines 9-37; 01-RESEARCH.md FRZ-04 disposition table; 01-PATTERNS.md input-validation and immutable-resource sections</read_first>
  <action>Extend ResolveFormats and the ResolveBaseFormat mirror with the remaining researcher-table arms RGB16F, RGB32F, RGBA32F, Depth16, Depth24, Depth32F, Depth24Stencil8, Depth32FStencil8, R8, R16F, R32F, verifying each internal-format and type pairing against glad.h tokens and spec tables and pairing each with its correct upload type plus bytes-per-pixel plus a separate block-size path for compressed sizes; make SetData and SetSubData type-aware so size validation uses the per-format size instead of byte assumptions; add S3TC arms for BC1_RGB, BC1_RGBA, BC3_RGBA that upload pre-compressed blocks via glCompressedTexImage2D only when GLAD_GL_EXT_texture_compression_s3tc is set and otherwise return failure with a PYRAMID_LOG_ERROR message naming the missing extension; execute the Task 2 decision for BC7_RGBA by pruning the enum value on approve or adding a loud explicit-failure arm on reject; keep None as a rejected sentinel at every public entry point; document single-channel read-as-RGBA and packed depth-stencil sampling behavior in docs/Architecture.md textures section (this task is the single Wave-1 owner of docs/Architecture.md); record the BC7-fate plus S3TC-gate changelog entry text in the 01-02 SUMMARY for consolidation into CHANGELOG.md by plan 01-04, and do not edit CHANGELOG.md in this plan; extend tests with per-format round trips plus malformed, truncated, very-large-extent, zero-extent, and S3TC-absent fixtures that must fail explicitly never crash, with the zero-extent creation case additionally carrying an explicit diagnostic and never producing a crash or empty handle; run ResourceHandleTests, ResourceManifestTests, and ResourceRegistryTests because cache-adjacent identity changed; publish new format resources only through ResourceRegistry Textures with no in-place mutation of cached instances and no parallel upload ownership.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Texture|Resource" }</automated></verify>
  <acceptance_criteria>Source assertions: every TextureFormat value except None either has a verified mapping arm or is pruned/gated with a diagnostic; S3TC arms contain the runtime flag check; no hardcoded GL_UNSIGNED_BYTE remains on any non-8-bit upload path; docs note single-channel and depth-stencil sampling behavior. Behavior assertions: texture plus cache plus handle plus manifest plus registry suites pass; malformed, truncated, very-large-extent, and zero-extent fixtures fail explicitly with the zero-extent case carrying a diagnostic; S3TC-absent path fails explicitly; changelog entry text is recorded in the 01-02 SUMMARY for plan 01-04 consolidation; engine plus tests build with zero errors.</acceptance_criteria>
  <done>Every advertised format maps with type-correct uploads, BC7 fate is resolved with a changelog, S3TC is gated, and no unmapped value reaches agent-visible API.</done>
  <reversibility rating="one-way">BC7 enum deletion breaks the published texture contract per D-03; gated by the Task 2 checkpoint.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-extents→texture-creation | Width, height, and format arrive from callers and may be hostile or corrupt |
| driver-capabilities→compressed-upload | S3TC support varies by driver; assuming it is undefined behavior |
| cache-identity→content-addressing | Byte-size mistakes corrupt dedup identity and handle resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of service | OpenGLTexture2D creation, TextureResource sizing | medium | mitigate | Reuse IsValidExtent plus CalculateByteSize overflow guards on every new path; very-large-extent tests must fail explicitly never crash |
| T-02-02 | Tampering | S3TC upload arms | medium | mitigate | Runtime GLAD_GL_EXT_texture_compression_s3tc check before glCompressedTexImage2D; absent path logs explicit error and fails; forced-absent test proves it |
| T-02-03 | Tampering | SetData / SetSubData | medium | mitigate | Type-aware size validation per format with separate block-size path for compressed; mismatch rejects explicitly |
| T-02-04 | Spoofing | cache identity for new formats | low | mitigate | Mirror every arm in ResolveBaseFormat with matching bytes-per-pixel; run handle, manifest, and registry suites; no in-place mutation of cached instances |
| T-02-SC | Tampering | package installs | low | accept | No package installs in this plan; sole runtime library is vendored vendor/glad, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
Texture, cache, handle, manifest, and registry suites green on debug; per-format round trips plus malformed, truncated, oversized, and S3TC-absent fixtures all behave explicitly; full debug build warning-clean per existing flags.
</verification>

<success_criteria>
An agent using any advertised texture format gets a working type-correct mapping; BC7 fate is resolved and documented; S3TC without driver support fails loudly; no unmapped enum value reaches agent-visible API (FRZ-04 satisfied).
</success_criteria>

<artifacts_produced>
Symbols created: none new public API (mapping arms are behavior inside existing ResolveFormats, ResolveBaseFormat, SetData, SetSubData). Symbols removed on Task 2 approve: TextureFormat::BC7_RGBA. Test artifacts: per-format round-trip plus malformed, truncated, oversized, zero-extent, and S3TC-absent cases in Tests/TextureLoadingTests.cpp and Tests/TextureCacheTests.cpp. Docs entry in docs/Architecture.md textures section; changelog entry text in the 01-02 SUMMARY consolidated into CHANGELOG.md by plan 01-04.
</artifacts_produced>

<output>
Create `.planning/phases/01-p0-core-freeze/01-02-SUMMARY.md` when done
</output>
