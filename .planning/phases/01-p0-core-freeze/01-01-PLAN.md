---
phase: 01-p0-core-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Engine/Graphics/include/Pyramid/Graphics/Renderer/RenderSystem.hpp
  - Engine/Graphics/source/Renderer/CommandBuffer.cpp
  - Engine/Graphics/include/Pyramid/Graphics/Shader/Shader.hpp
  - Engine/Graphics/source/OpenGL/Shader/OpenGLShader.cpp
  - Engine/Graphics/include/Pyramid/Graphics/Scene/SceneManager.hpp
  - Engine/Graphics/source/Scene/SceneManager.cpp
  - Tests/PublicApiLinkage.cpp
  - docs/ROADMAP.md
  - CHANGELOG.md
autonomous: true
requirements: [FRZ-01, FRZ-03]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Dispatching compute work through the documented command model is impossible to express silently: the Dispatch symbol does not exist, so misuse fails at compile time, never as a log-and-drop at runtime (FRZ-01, per D-01)"
    - "Enabling occlusion culling is impossible to express silently: the setting does not exist, so visibility is observably frustum plus octree only (FRZ-03, per D-02)"
    - "The public-API linkage suite pins the removal: no removed symbol remains referenced, and the suite is green (FRZ-01, FRZ-03)"
    - "ASSUMED (unresolved specless probe FRZ-01/unclassified, manual review): removal rather than keep-reserved-with-explicit-failure is the correct fate; no consumer in Examples, Tests, or renderer needs GPU compute before a 4.3-backend decision"
    - "ASSUMED (unresolved specless probe FRZ-03/unclassified, manual review): frustum plus octree pruning fully carries visibility; no agent-observable behavior depended on the occlusion flag (it always returned false)"
  artifacts:
    - "Engine/Graphics/include/Pyramid/Graphics/Renderer/RenderSystem.hpp without Dispatch"
    - "Engine/Graphics/source/Renderer/CommandBuffer.cpp without the Dispatch record and drop arm"
    - "Engine/Graphics/include/Pyramid/Graphics/Shader/Shader.hpp without CompileCompute and DispatchCompute"
    - "Engine/Graphics/source/OpenGL/Shader/OpenGLShader.cpp without the compute implementation"
    - "Engine/Graphics/include/Pyramid/Graphics/Scene/SceneManager.hpp without occlusion members"
    - "Engine/Graphics/source/Scene/SceneManager.cpp without OcclusionCull and its filter block"
    - "Tests/PublicApiLinkage.cpp reflecting every removed symbol"
  key_links:
    - "RenderCommandType enum to CommandBuffer Execute switch (no orphan case arm remains)"
    - "IShader interface to OpenGLShader implementation (no dangling override remains)"
    - "SceneManager header declarations to cpp definitions (no dangling declaration remains)"
    - "Removed symbols to Tests/PublicApiLinkage.cpp pins (linkage suite is the removal verification)"
  prohibitions:
    - "Never leave an advertised capability silently unperformed"
    - "Never add a new engine capability disguised as a freeze fix"
    - "Never mutate published immutable resources in place"
---

<objective>
Remove the two log-only/placeholder surfaces outright so agents get compile-time signals instead of silent misrepresentation. Per D-01 (FRZ-01, researcher recommendation REMOVE: OpenGL 3.3 core has no compute shaders, glDispatchCompute needs 4.3+) and per D-02 (FRZ-03, researcher recommendation REMOVE: correct GPU queries need async plumbing SceneManager lacks, a new subsystem contradicting the no-new-capabilities boundary).

Purpose: Frozen ground for agents — every P0 stub is gone with rationale, never silently kept.
Output: Six engine files with the dead surface deleted, linkage suite updated, docs rationale entries, green build and tests.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p0-core-freeze/01-CONTEXT.md
@.planning/phases/01-p0-core-freeze/01-RESEARCH.md
@.planning/phases/01-p0-core-freeze/01-PATTERNS.md
@.planning/phases/01-p0-core-freeze/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Remove compute-dispatch surface end to end</name>
  <files>Engine/Graphics/include/Pyramid/Graphics/Renderer/RenderSystem.hpp, Engine/Graphics/source/Renderer/CommandBuffer.cpp, Engine/Graphics/include/Pyramid/Graphics/Shader/Shader.hpp, Engine/Graphics/source/OpenGL/Shader/OpenGLShader.cpp, Tests/PublicApiLinkage.cpp</files>
  <read_first>Engine/Graphics/include/Pyramid/Graphics/Renderer/RenderSystem.hpp lines 50-92 and 145; Engine/Graphics/source/Renderer/CommandBuffer.cpp lines 259-269 and 560-567; Engine/Graphics/include/Pyramid/Graphics/Shader/Shader.hpp lines 67-80; Engine/Graphics/source/OpenGL/Shader/OpenGLShader.cpp lines 558-573; Tests/PublicApiLinkage.cpp lines 197-249; 01-PATTERNS.md RenderSystem/CommandBuffer and PublicApiLinkage sections</read_first>
  <action>Delete RenderCommandType Dispatch enum value plus the dispatch union member plus the Dispatch declaration in RenderSystem.hpp; delete the CommandBuffer Dispatch record method plus the Execute Dispatch case arm while keeping the default loud-WARN arm; delete IShader CompileCompute and DispatchCompute pure-virtual declarations plus the OpenGLShader definitions; audit BindShaderStorageBuffer, SetShaderStorageBlockBinding, and CreateShaderStorageBuffer and remove them only if compute-exclusive, otherwise keep generic buffer objects with a comment stating why they stay; delete the linkage pin lines for each removed symbol in Tests/PublicApiLinkage.cpp; never replace the deletion with a log-and-drop or a silent no-op default per D-01; follow C++17 four-space braces-on-new-lines style with PascalCase methods and m_ fields. Per D-01 researcher recommendation REMOVE.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "PublicApi|CommandBuffer" }</automated></verify>
  <acceptance_criteria>Source assertions: no occurrence of DispatchCompute, CompileCompute, or RenderCommandType::Dispatch remains in Engine/ or Tests/ (verified with a repo-wide search excluding CHANGELOG and docs rationale text); the Execute switch retains its default WARN arm. Behavior assertions: full engine plus tests build with zero errors; PublicApiLinkage suite passes; CommandBufferStatsTests passes unchanged.</acceptance_criteria>
  <done>Compute dispatch is inexpressible in code: any agent attempt fails at compile time, and the linkage suite proves the removal.</done>
  <reversibility rating="costly">Re-adding Dispatch re-opens the command-model contract per D-01; removal is the researcher-recommended fate.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Remove occlusion-culling placeholder end to end</name>
  <files>Engine/Graphics/include/Pyramid/Graphics/Scene/SceneManager.hpp, Engine/Graphics/source/Scene/SceneManager.cpp</files>
  <read_first>Engine/Graphics/include/Pyramid/Graphics/Scene/SceneManager.hpp lines 162-177 and 191-194; Engine/Graphics/source/Scene/SceneManager.cpp lines 277-311 and 541-547; 01-PATTERNS.md SceneManager section</read_first>
  <action>Delete SetOcclusionCullingEnabled inline setter plus the OcclusionCull declaration plus the m_occlusionCullingEnabled member while keeping the neighboring SetFrustumCullingEnabled authority; delete the occlusion filter block in GetVisibleObjects plus the OcclusionCull stub definition that unconditionally returns false; run a repo-wide search for SetOcclusionCullingEnabled, m_occlusionCullingEnabled, and OcclusionCull across Engine, Examples, and Tests and delete or update every reference found so no dangling call site remains; if Tests/PublicApiLinkage.cpp pins the setter, remove that pin line in this task; do not add query objects, proxy geometry, or any replacement technique per D-02. Per D-02 researcher recommendation REMOVE.</action>
  <verify><automated>cmake --build --preset build-gcc-debug-tests; if ($?) { ctest --preset test-gcc-debug -R "Scene|Frustum|Octree|Culling|PublicApi" }</automated></verify>
  <acceptance_criteria>Source assertions: no occurrence of OcclusionCull, occlusionCullingEnabled, or SetOcclusionCullingEnabled remains in Engine, Examples, or Tests (verified with a repo-wide search excluding docs rationale text). Behavior assertions: frustum-culling, octree, and scene test suites pass unchanged, proving visibility parity rests on frustum plus octree; engine plus tests build with zero errors.</acceptance_criteria>
  <done>Occlusion culling is inexpressible in code: visibility is observably frustum plus octree only, with suites proving no regression.</done>
  <reversibility rating="costly">Re-adding the setting is a new API decision per D-02; removal is the researcher-recommended fate.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record removal rationale and prove contract honesty</name>
  <files>docs/ROADMAP.md, CHANGELOG.md</files>
  <read_first>docs/ROADMAP.md lines 38-86; CHANGELOG.md head entries for prior removal-entry shape</read_first>
  <action>Add one-line removal rationale entries in the docs/ROADMAP.md P0 section for compute (requires OpenGL 4.3-plus, baseline is 3.3, re-add only with a higher-baseline backend decision) and occlusion (visibility equals frustum plus octree, occlusion deferred until a device-side query technique earns its own phase), plus matching CHANGELOG.md entries noting the deleted symbols; run repo-wide absence searches for all removed identifiers and report counts of zero; run the full debug test suite as the phase-contract proof for this plan.</action>
  <verify><automated>ctest --preset test-gcc-debug</automated></verify>
  <acceptance_criteria>Source assertions: docs/ROADMAP.md P0 contains one rationale line each for the compute and occlusion removals; CHANGELOG.md contains matching entries naming the deleted symbols. Behavior assertions: full ctest debug suite is green; absence searches return zero matches for DispatchCompute, CompileCompute, OcclusionCull, and SetOcclusionCullingEnabled outside docs and changelog text.</acceptance_criteria>
  <done>Rationale is documented where agents look, and the full suite proves nothing on the agent path still references the removed surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-code→engine-headers | Agent code includes engine headers; removed symbols must fail at compile time, never link to stale behavior |
| engine-source→linkage-suite | Tests/PublicApiLinkage.cpp pins the public surface; drift must fail loudly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | RenderSystem.hpp / CommandBuffer.cpp | low | mitigate | Delete enum, union member, record, and case arm together; verify with repo-wide absence search plus full build |
| T-01-02 | Tampering | Shader.hpp / OpenGLShader.cpp | low | mitigate | Delete interface and implementation together; SSBO audit keeps only non-compute-exclusive buffer objects; linkage suite proves surface |
| T-01-03 | Denial of service | SceneManager visibility path | low | mitigate | Keep frustum plus octree authority untouched; existing frustum/octree suites must pass unchanged proving parity |
| T-01-04 | Repudiation | docs/ROADMAP.md, CHANGELOG.md | low | accept | Rationale lines record why removal happened and under what condition re-adding is legitimate |
| T-01-SC | Tampering | package installs | low | accept | No package installs in this plan; sole runtime library is vendored vendor/glad, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
Full debug build plus full ctest debug suite green; repo-wide absence searches for all removed identifiers return zero outside docs text; linkage suite green.
</verification>

<success_criteria>
An agent can no longer name compute dispatch or occlusion culling in code — misuse fails at compile time; docs state why and what would justify re-adding; every FRZ-01 and FRZ-03 success-criterion clause for removal is satisfied.
</success_criteria>

<artifacts_produced>
Symbols removed (not created): RenderCommandType::Dispatch, RenderCommand dispatch union member, CommandBuffer::Dispatch, IShader::CompileCompute, IShader::DispatchCompute, OpenGLShader compute definitions, SceneManager::SetOcclusionCullingEnabled, SceneManager::OcclusionCull, SceneManager::m_occlusionCullingEnabled, plus compute-exclusive SSBO helpers if the audit finds them exclusive. Symbols created: none — this plan deletes surface. Docs entries in docs/ROADMAP.md P0 and CHANGELOG.md.
</artifacts_produced>

<output>
Create `.planning/phases/01-p0-core-freeze/01-01-SUMMARY.md` when done
</output>
